snippet initialscript
	#include <bits/stdc++.h>
	
	using namespace std;
	
	int main()
	{
		${0}

		return 0;
	}

snippet gcd
	long long gcd(long long a, long long b) {
		if (b == 0) return a;
		else return gcd(b, a % b);
	}
